# === Rust / Cargo ===
# Ignore compiled artifacts and build outputs
/target/
debug/ # Older build directory, sometimes used

# Ignore dependency resolution file (often committed, but can be ignored in some workflows)
# Cargo.lock

# Ignore backup files generated by rustfmt
**/*.rs.bk

# Ignore MSVC Windows debugging information files
*.pdb

# === Editor / IDE ===
# Ignore editor configuration files
.idea/
.vscode/
*.swp
*~
*.bak

# === OS Specific ===
# Ignore macOS specific files
.DS_Store
# Ignore Windows specific files
Thumbs.db

# === DevRS Specific ===
# Ignore environment files if they contain secrets
*.env

# Ignore the active/customized DevRS config file in presets
presets/config.toml

# Ignore logs
*.log

# Ignore specific tool outputs if necessary
# e.g., coverage reports
/coverage/
*.profraw

# Ignore user configuration if potentially checked in by accident
# (though it should live in ~/.config/devrs)
# config.toml

# Ignore potentially generated blueprint outputs if testing locally
# /path/to/test_output/

# Ignore Neovim session files if any are created locally
*.session

# === src/CMakeLists.txt ===
# Includes the library and executable subdirectories

# Add the chatbot library subdirectory
add_subdirectory(chatbot_lib)

# Add the main executable subdirectory
add_subdirectory(main)


